export const metadata = {
    title: 'Quiz | User Journey',
    description: 'Test your knowledge',
}

export default function QuizLayout({ children }) {
    return (
        <section>
            {children}
        </section>
    )
}
